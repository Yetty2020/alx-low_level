#include "main.h"
#include <stdio.h>
/**
 * main - checkk the code
 * Return: Always 0
 */
int main(void)
{
	char *dest[12] = "Hello";
	char *src[n] = "World";
	int n = 12;

	_strncat(*dest, *src);
	printf("_strncat(*dest, *src) : %s\n", *dest);
	return (0);

}
