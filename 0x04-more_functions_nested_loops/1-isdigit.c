#include "main.h"
/**
 * main - check the code
 * _isdigit - checks for a digit (0 to 9)
 * @c - integer to be checked
 * Return: wheter it is 1, 0 otherwide
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
