#include "main.h"
/**
 * add - adds two integers and returns the result
 * a - integer to be added to a
 * b - integer to e added to b
 * Return: sum
 */
int add(int a, int b)
{
	int c;

	c = a + b;
	return (c);
}
